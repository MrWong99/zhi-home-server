nginx-proxy-manager:
  image-tag:
    val: "latest"
    metadata:
      ui.section: "Image"
      ui.displayName: "NPM Image Tag"
      core.description: "Docker image tag for jc21/nginx-proxy-manager"
      core.type: string
  http-port:
    val: 80
    metadata:
      ui.section: "Ports"
      ui.displayName: "HTTP Port"
      core.description: "Host port for HTTP traffic"
      core.type: int
  https-port:
    val: 443
    metadata:
      ui.section: "Ports"
      ui.displayName: "HTTPS Port"
      core.description: "Host port for HTTPS traffic"
      core.type: int
  admin-port:
    val: 81
    metadata:
      ui.section: "Ports"
      ui.displayName: "Admin UI Port"
      core.description: "Host port for NPM admin web interface"
      core.type: int
  letsencrypt-email:
    val: ""
    metadata:
      ui.section: "SSL"
      ui.displayName: "Let's Encrypt Email"
      core.description: "Email for Let's Encrypt certificate notifications"
      core.type: string
      ui.placeholder: "admin@example.com"
